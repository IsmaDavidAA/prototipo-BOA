import React from "react";
import useGlobal from "../hooks/useGlobal";

export default function RegistroPasajero() {
  const { userDef, setUserDef } = useGlobal();

  return (
    <div>
      <h1>RegistroPasajero</h1>
    </div>
  );
}
